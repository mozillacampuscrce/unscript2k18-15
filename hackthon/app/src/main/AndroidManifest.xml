<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.collekarry.hackthon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".changeUsr" />
        <activity android:name=".eventDetails" />
        <activity android:name=".ManageEvent" />
        <activity android:name=".EventView" />
        <activity android:name=".StudentChoice" />
        <activity android:name=".outsideStudents" />
        <activity android:name=".insideStudent" />
        <activity android:name=".CalenderEdit" />
        <activity android:name=".GotPlace" />
        <activity android:name=".StudentPermission"></activity>
    </application>

</manifest>